# Packet Tracer – Implement DHCPv4

> **Objectif** : Exercice de synthèse DHCPv4 — configurer R2 comme serveur DHCP, R1 et R3 comme relays, et R2 comme client DHCP vers l'ISP. Topologie et tâches identiques au PT 7.2.10, sans les explications intermédiaires.

---

## Topologie

| Device     | Interface | Adresse IP      | Masque          | Passerelle    |
|------------|-----------|-----------------|-----------------|---------------|
| R1         | G0/0      | 192.168.10.1    | 255.255.255.0   | N/A           |
| R1         | S0/0/0    | 10.1.1.1        | 255.255.255.252 | N/A           |
| R2         | G0/0      | 192.168.20.1    | 255.255.255.0   | N/A           |
| R2         | G0/1      | DHCP (ISP)      | DHCP (ISP)      | N/A           |
| R2         | S0/0/0    | 10.1.1.2        | 255.255.255.252 | N/A           |
| R2         | S0/0/1    | 10.2.2.2        | 255.255.255.252 | N/A           |
| R3         | G0/0      | 192.168.30.1    | 255.255.255.0   | N/A           |
| R3         | S0/0/1    | 10.2.2.1        | 255.255.255.252 | N/A           |
| PC1        | NIC       | DHCP            | DHCP            | DHCP          |
| PC2        | NIC       | DHCP            | DHCP            | DHCP          |
| DNS Server | NIC       | 192.168.20.254  | 255.255.255.0   | 192.168.20.1  |

---

## Partie 1 : R2 comme serveur DHCP

```
R2> enable
R2# configure terminal

R2(config)# ip dhcp excluded-address 192.168.10.1 192.168.10.10
R2(config)# ip dhcp excluded-address 192.168.30.1 192.168.30.10

R2(config)# ip dhcp pool R1-LAN
R2(dhcp-config)# network 192.168.10.0 255.255.255.0
R2(dhcp-config)# default-router 192.168.10.1
R2(dhcp-config)# dns-server 192.168.20.254
R2(dhcp-config)# exit

R2(config)# ip dhcp pool R3-LAN
R2(dhcp-config)# network 192.168.30.0 255.255.255.0
R2(dhcp-config)# default-router 192.168.30.1
R2(dhcp-config)# dns-server 192.168.20.254
R2(dhcp-config)# exit

R2(config)# end
```

---

## Partie 2 : DHCP Relay sur R1 et R3

```
R1> enable
R1# configure terminal

R1(config)# interface g0/0
R1(config-if)# ip helper-address 10.1.1.2
R1(config-if)# exit

R1(config)# end
```

```
R3> enable
R3# configure terminal

R3(config)# interface g0/0
R3(config-if)# ip helper-address 10.2.2.2
R3(config-if)# exit

R3(config)# end
```

**PC1 et PC2** : Desktop → IP Configuration → **DHCP**

---

## Partie 3 : R2 comme client DHCP

```
R2> enable
R2# configure terminal

R2(config)# interface g0/1
R2(config-if)# ip address dhcp
R2(config-if)# no shutdown
R2(config-if)# exit

R2(config)# end

R2# show ip interface brief
! → G0/1 : IP assignée par ISP, UP/UP ✅
```

---

## Vérification

```
R2# show ip dhcp binding
! → 192.168.10.11 (PC1, pool R1-LAN)
! → 192.168.30.11 (PC2, pool R3-LAN)
```

---

## Résumé des commandes clés

| Commande | Rôle |
|----------|------|
| `ip dhcp excluded-address <début> <fin>` | Exclut une plage d'IPs du pool |
| `ip dhcp pool <nom>` | Crée un pool DHCP (sensible à la casse) |
| `network <réseau> <masque>` | Réseau distribué par le pool |
| `default-router <ip>` | Passerelle envoyée aux clients |
| `dns-server <ip>` | DNS communiqué aux clients |
| `ip helper-address <ip>` | Relay DHCP sur l'interface LAN du routeur |
| `ip address dhcp` | Configure l'interface comme client DHCP |
| `show ip dhcp binding` | Affiche les baux DHCP actifs |

---

## Script complet (toutes les commandes dans l'ordre)

```
! ══════════════════════════════════════════════
! ─── PARTIE 1 : R2 serveur DHCP ───────────────
! ══════════════════════════════════════════════

    enable

        configure terminal

            ! ─── Global Config ───────────────
            ip dhcp excluded-address 192.168.10.1 192.168.10.10
            ip dhcp excluded-address 192.168.30.1 192.168.30.10

            ip dhcp pool R1-LAN
                ! ─── DHCP Config ─────────────
                network 192.168.10.0 255.255.255.0
                default-router 192.168.10.1
                dns-server 192.168.20.254
                exit

            ip dhcp pool R3-LAN
                ! ─── DHCP Config ─────────────
                network 192.168.30.0 255.255.255.0
                default-router 192.168.30.1
                dns-server 192.168.20.254
                exit

            end


! ══════════════════════════════════════════════
! ─── PARTIE 2 : DHCP Relay ────────────────────
! ══════════════════════════════════════════════

! ─── Sur R1 ───────────────────────────────────

    enable

        configure terminal

            ! ─── Global Config ───────────────
            interface g0/0
                ! ─── Interface Config ────────
                ip helper-address 10.1.1.2
                exit

            end


! ─── Sur R3 ───────────────────────────────────

    enable

        configure terminal

            ! ─── Global Config ───────────────
            interface g0/0
                ! ─── Interface Config ────────
                ip helper-address 10.2.2.2
                exit

            end


! ─── PCs : configuration via GUI ──────────────
! PC1 : Desktop > IP Configuration > DHCP
! PC2 : Desktop > IP Configuration > DHCP


! ══════════════════════════════════════════════
! ─── PARTIE 3 : R2 client DHCP ────────────────
! ══════════════════════════════════════════════

    enable

        configure terminal

            ! ─── Global Config ───────────────
            interface g0/1
                ! ─── Interface Config ────────
                ip address dhcp
                no shutdown
                exit

            end

        show ip interface brief
        ! → G0/1 UP/UP avec IP ISP ✅

        show ip dhcp binding
        ! → PC1 : 192.168.10.11 (R1-LAN)
        ! → PC2 : 192.168.30.11 (R3-LAN)
```